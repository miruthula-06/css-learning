# Selector

Firstly we have 3 types of css

1.Inline css

2.Internal css

3.external css

Priority wise in CSS

Inline>Internal>External

In this 3 types we have to take first priority in Inline

For example:

1.In same file we have apply 3 css but take inline so I can understand in first priority is Inline

2.In same file we have apply 2 css one is Internal and another one is external it will take internal

Secondly Selectors

3 selector

Id, Class and element

Priority wise

Id>Class>Element

In same file one element we can apply different selector but it will take priority wise

In same file one element we can apply same selector but it will take lastly execution selector

for example

In same file I am using p element in external css file we have to give same selector means it will take last eecution (Id,class and Element)

In same file I am using p element in external css file we have to give different selector means it will take (Id,Class,Element) in this format I will give and take it Id selector